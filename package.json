{
  "name": "keepward",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate:atlas && tsc && vite build",
    "preview": "vite preview",
    "generate:atlas": "node scripts/generate-atlas.mjs"
  },
  "devDependencies": {
    "@types/pngjs": "^6.0.5",
    "pngjs": "^7.0.0",
    "typescript": "~5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0"
  },
  "dependencies": {
    "phaser": "3.87.0"
  }
}
